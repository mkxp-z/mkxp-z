# I have no idea what I'm doing
name: Build Gem

on:
  push:
    branches: ['dev', 'build-gem']
    tags: ['*']
    paths-ignore: ['*.md', '*.json', '*.png']
  pull_request:
    branches: ['dev', 'build-gem']
    paths-ignore: ['*.md', '*.json', '*.png']

jobs:
  build-windows:
    name: Windows - Build
    runs-on: windows-latest

    strategy:
      matrix:
        ruby: ['3.1', '3.2']
        msys:
          - { sys: mingw64, env: x86_64, clib: msvcrt }
          - { sys: ucrt64,  env: ucrt-x86_64, clib: ucrt }

    steps:
      - uses: benjlevesque/short-sha@v2.2
        id: short-sha
        with:
          length: 7

      - uses: msys2/setup-msys2@v2
        with:
          msystem: ${{matrix.msys.sys}}
          install: >-
            base-devel
            git
            vim
            mingw-w64-${{matrix.msys.env}}-cmake
            mingw-w64-${{matrix.msys.env}}-meson
            mingw-w64-${{matrix.msys.env}}-autotools
            mingw-w64-${{matrix.msys.env}}-gcc

      - name: Install Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}

      - name: Export Ruby Location Variables
        id: ruby_path
        shell: powershell
        run: |
          $ruby_root = "$("$(Get-Command Ruby | Resolve-Path)/../.." | Resolve-Path)".replace('([A-Z]):', '/$1').replace('\', '/')
          echo "RUBY_BIN_DIR=$ruby_root/bin" >> $GITHUB_OUTPUT
          echo "RUBY_LIB_DIR=$ruby_root/lib" >> $GITHUB_OUTPUT
          echo "RUBY_LIB_NAME=x64-${{matrix.msys.clib}}-ruby$("${{matrix.ruby}}".replace(".", ""))0" >> $GITHUB_OUTPUT
          $RUBY_INCLUDE_DIR = "$ruby_root/include/ruby-${{matrix.ruby}}.0"
          echo "RUBY_INCLUDE_DIRS=${RUBY_INCLUDE_DIR}:${RUBY_INCLUDE_DIR}/x64-mingw-${{matrix.msys.clib}}" >> $GITHUB_OUTPUT
          

      - name: Checkout repository
        uses: actions/checkout@v3

      - uses: actions/cache@v3
        with:
          path: |
            windows/build-mingw64
            windows/downloads
          key: win-gem-${{matrix.ruby}}-${{matrix.msys.sys}}-${{ hashFiles('windows/Makefile') }}

      - name: Build all dependencies
        shell: msys2 {0}
        env:
          RUBY_BIN_DIR: ${{steps.ruby_path.output.RUBY_BIN_DIR}}
        run: |
          cd windows
          export $PATH=$PATH:$RUBY_BIN_DIR
          make deps-core

      - name: Build Ruby gem
        shell: msys2 {0}
        env:
          RUBY_BIN_DIR: ${{steps.ruby_path.output.RUBY_BIN_DIR}}
          RUBY_LIB_DIR: ${{steps.ruby_path.output.RUBY_LIB_DIR}}
          RUBY_LIB_NAME: ${{steps.ruby_path.output.RUBY_LIB_NAME}}
          RUBY_INCLUDE_DIRS: ${{steps.ruby_path.output.RUBY_INCLUDE_DIRS}}
        run: |
          source windows/vars.sh
          export $PATH=$PATH:$RUBY_BIN_DIR
          meson build -Dbuild_gem=true -Dmri_includes=$RUBY_INCLUDE_DIRS -Dmri_libpath=$RUBY_LIB_DIR -Dmri_library=$RUBY_LIB_NAME
          cd build
          ninja install
          strip ./mkxpz.so
          cd ..
          gem build mkxp-z.gemspec
          dirname $(where gcc) >> $GITHUB_PATH

      - name: Install sonar-scanner and build-wrapper
        if: ${{matrix.msys.sys == 'mingw64'}}
        uses: SonarSource/sonarcloud-github-c-cpp@v2

      - name: Run sonar-scanner
        if: ${{matrix.msys.sys == 'mingw64'}}
        env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          sonar-scanner --define sonar.cfamily.compile-commands=build/compile_commands.json --define sonar.projectKey=fcorso2016_mkxp-z_gem_windows  

      - name: Prepare archive
        shell: msys2 {0}
        run: |
          mkdir build/artifact
          cd build/artifact
          cp /${{ matrix.sys }}/bin/zlib1.dll .
          cp ../../mkxp-z-*.gem .
          cp ../../mkxp.json .
          cp -r ../../scripts .
          cp -r ../../tests .

      - uses: actions/upload-artifact@v3
        with:
          name: mkxp-z.windows.gem.${{matrix.ruby}}.${{matrix.msys.sys}}.${{github.event_name == 'pull_request' && format('PR{0}', github.event.number) || github.ref_name}}-${{steps.short-sha.outputs.sha}}
          path: |
            build/artifact/*.dll
            build/artifact/*.gem
            build/artifact/mkxp.json
            build/artifact/scripts/


  test-windows:
    name: Windows - Test
    runs-on: windows-latest
    needs: build-windows
    steps:
      - uses: benjlevesque/short-sha@v2.2
        id: short-sha
        with:
          length: 7

      - name: Install Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'

      - name: Download Tests
        uses: snow-actions/sparse-checkout@v1.2.0
        with:
          patterns: |
            tests
            Rakefile

      - name: Download Built Gem
        uses: actions/download-artifact@v3
        with:
          name: mkxp-z.windows.gem.ucrt64.${{github.event_name == 'pull_request' && format('PR{0}', github.event.number) || github.ref_name}}-${{steps.short-sha.outputs.sha}}

      - name: Install Gem
        run: |
          cd build/artifact
          $gem_file = @(gci mkxp-z-*.gem)[0]
          gem install ${gem_file}
          echo "${pwd}" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Test Gem
        run: |
          cd ../..
          rake

  build-linux:
    name: Ubuntu 22.04 - Build
    runs-on: ubuntu-22.04
    steps:
      - uses: benjlevesque/short-sha@v2.2
        id: short-sha
        with:
          length: 7

      - name: Checkout repository
        uses: actions/checkout@v3

      - uses: actions/cache@v3
        with:
          path: |
            linux/build-x86_64
            linux/downloads
          key: lin-gem-${{ hashFiles('linux/Makefile') }}

      - name: Install apt dependencies
        run: |
          sudo apt update
          sudo apt install git build-essential cmake meson autoconf automake libtool pkg-config ruby bison zlib1g-dev libbz2-dev xorg-dev libgl1-mesa-dev libasound2-dev libpulse-dev -y

      - name: Build everything else
        env:
          CMAKE_EXTRA_ARGS: -DCMAKE_POSITION_INDEPENDENT_CODE=ON
          EXTRA_CONFIG_OPTIONS: --with-pic
        run: |
          cd linux
          make

      - name: Build Ruby gem
        run: |
          source linux/vars.sh
          meson build --bindir=. --prefix=$GITHUB_WORKSPACE/build/local -Dbuild_gem=true
          cd build
          ninja
          ninja install
          cd ..
          gem build mkxp-z.gemspec

      - name: Install sonar-scanner and build-wrapper
        uses: SonarSource/sonarcloud-github-c-cpp@v2

      - name: Run sonar-scanner
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          sonar-scanner --define sonar.cfamily.compile-commands=build/compile_commands.json --define sonar.projectKey=fcorso2016_mkxp-z_gem_linux

      - name: Prepare archive
        run: |
          cd build/local
          cp ../../mkxp-z-*.gem .
          cp ../../mkxp.json .
          cp -r ../../scripts .
          cd ..
          zip -r local.zip local

      - uses: actions/upload-artifact@v3
        with:
          name: mkxp-z.linux.${{github.event_name == 'pull_request' && format('PR{0}', github.event.number) || github.ref_name}}-${{steps.short-sha.outputs.sha}}
          path: build/local.zip
